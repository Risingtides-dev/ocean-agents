# CLAUDE.md — ocean-agents

Agent instructions live in **AGENTS.md** (cross-tool standard). Read it first.
Provider-agnostic agent packages (couriers + assistants) that run on top of the Ocean runtime — kept separate from `ocean-os` (pure Rust). Invoke couriers via `python3 couriers/hub/router.py run <slash> [args]` (`list` shows the live registry; the router is authoritative).

@AGENTS.md
